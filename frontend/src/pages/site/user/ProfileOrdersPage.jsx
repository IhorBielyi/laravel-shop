import React from "react";
import {Alert} from "@mui/material";

export default function ProfileOrdersPage() {
    return (
        <Alert severity="info">
            Замовлення скоро зʼявляться 🙂 (поки що цей розділ — заглушка)
        </Alert>
    );
}